version: "3.9"
services:
  test:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    command: pytest --maxfail=1 --disable-warnings -q
    depends_on:
      - db
      - redis
    volumes:
      - ./:/app
